import React from 'react'
import CardLayout from './Cards/CardLayout'



const Banner = ({width, height, bg}) => {
  return <CardLayout width={width} height={height} bg={bg}>

  </CardLayout>;
}

export default Banner